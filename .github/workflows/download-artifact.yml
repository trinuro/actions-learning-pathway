name: Download artifact
on: workflow_dispatch
jobs:
  download-and-read:
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: hello.txt
      - name: Read file
        run: cat hello.txt