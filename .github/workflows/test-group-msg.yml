name: "Workflow Commands"
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name : "Group logging"
        run: |
          echo "::group::Group Message example"
          echo "Msg 1"
          echo "Msg 2"
          echo "::endgroup::"